{
    "version": 2,
    "name": "VGrid",
    "builds": [
      { "src": "package.json", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.html" }
    ],
    "functions": { "api/*.js": { "maxDuration": 60 } }
  }