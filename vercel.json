{
    "version": 2,
    "name": "test",
    "builds": [
      { "src": "package.json", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.html" }
    ],
    "functions": { "app/api/*.js": { "maxDuration": 60 } }
  }